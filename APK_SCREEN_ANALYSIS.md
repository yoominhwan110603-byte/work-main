# Vinyl-Check APK 화면별 프레임 분석

기준 APK:

```text
C:\Users\User\OneDrive\Desktop\work-main\apps\mobile\android\app\build\outputs\apk\debug\app-debug.apk
```

기준 빌드 시간:

```text
2026-05-14 오후 10:58:39
```

분석 기준:

- `apps/mobile/src/router.ts`에 등록된 실제 라우트 기준
- `apps/mobile/src/views/*.vue`와 공통 컴포넌트 기준
- 실제 백엔드 연결 여부와 mock/localStorage 기반 동작 여부를 구분
- “상단 / 중간 / 하단”은 모바일 화면에서 사용자가 보는 시각적 영역 기준

---

## 1. 시작 화면

경로:

```text
/
```

상단:

- 별도 상단바 없음
- 전체 화면 그라데이션 배경

중간:

- Vinyl-Check 로고성 아이콘
- 앱 이름 `Vinyl-Check`
- 설명 문구
  - “1:1 중고 LP 거래 플랫폼”
  - 음질 분석 기반 거래 경험 안내

하단:

- `시작하기` 버튼
  - 회원가입 화면으로 이동
- `이미 계정이 있나요? 로그인` 버튼
  - 로그인 화면으로 이동

구현 상태:

- 화면 이동 구현됨
- 실제 서버 호출 없음

보완할 점:

- 없음. 시작 화면으로는 충분함.

---

## 2. 로그인 화면

경로:

```text
/auth/login
```

상단:

- 뒤로가기 버튼
  - 시작 화면으로 이동

중간:

- 제목: 로그인
- 설명 문구
- 서버 연결 상태 표시
  - 웹 로컬: `http://127.0.0.1:8000`
  - APK: `http://192.168.219.112:8000`
- 입력 필드
  - 이메일 또는 아이디
  - 비밀번호
- 로그인 유지 체크박스
- 오류/성공 메시지 표시 영역
- 일반 로그인 버튼
- Google 로그인 버튼
- 아이디 찾기 버튼
- 비밀번호 찾기 버튼

하단:

- “계정이 없나요?”
- 회원가입 버튼

연결 기능:

- 일반 로그인:

```text
POST /auth/login
```

- Google 로그인:

```text
POST /auth/google
```

- 서버 상태:

```text
GET /health
```

구현 상태:

- 일반 로그인 실제 백엔드 연결됨
- 아이디 또는 이메일로 로그인 가능
- APK에서는 Capgo Social Login 기반 Google 로그인 사용
- 웹 로컬에서는 개발 테스트용 Google 로그인 우회 처리
- 서버 연결 실패 시 API 주소가 포함된 메시지 표시

미구현/부분구현:

- 웹 브라우저에서 “진짜 Google 팝업 로그인”은 Google Cloud Web OAuth 설정이 필요함
- APK Google 로그인은 Android OAuth 설정에 의존함

보완안:

- Google Cloud Console에 Web OAuth Client를 추가하고 다음 Origin 등록

```text
http://localhost:5173
http://127.0.0.1:5173
```

- `.env.local`의 웹용 Client ID와 Android Client ID를 분리해서 관리

---

## 3. 회원가입 화면

경로:

```text
/auth/signup
```

상단:

- 뒤로가기 버튼
  - 로그인 화면으로 이동
- 제목: 회원가입

중간:

- 안내 문구
- 아이디 입력
- 중복 확인 버튼
- 이메일 입력
- 비밀번호 입력
- 비밀번호 확인 입력
- 비밀번호 불일치 안내
- 중복 확인/오류 메시지 표시

하단:

- 다음 버튼
  - 중복 확인과 비밀번호 확인이 완료되어야 활성화

연결 기능:

```text
POST /auth/check
```

구현 상태:

- 아이디/이메일 중복 확인 실제 백엔드 연결됨
- 중복 확인 후 선호 장르 화면으로 이동
- 회원가입 정보는 임시로 `sessionStorage`에 저장 후 다음 화면에서 최종 가입

미구현/부분구현:

- 이메일 인증 절차는 실제 발송/확인 플로우가 없음

보완안:

- 이메일 인증 코드 발송 API 추가
- 회원가입 완료 전 이메일 인증 필수 여부 결정

---

## 4. 선호 장르 선택 화면

경로:

```text
/auth/preference
```

상단:

- 별도 헤더 없음
- 제목과 안내 문구가 상단 영역에 배치

중간:

- 선호 장르 버튼 그리드
- 최대 5개 선택 가능
- 선택 수 표시
- 오류 메시지 표시

하단:

- 완료 버튼
- 건너뛰기 버튼

연결 기능:

```text
POST /auth/signup
```

구현 상태:

- 실제 회원가입 API 연결됨
- 선택 장르가 사용자 프로필에 저장됨
- draft가 없으면 회원가입 화면으로 돌려보냄

미구현/부분구현:

- 장르 텍스트 일부는 빌드/콘솔 출력에서 인코딩이 깨져 보일 수 있음

보완안:

- 전체 소스 파일 인코딩을 UTF-8로 정리
- 장르 목록을 상수 파일로 분리

---

## 5. 홈 화면

경로:

```text
/app
```

상단:

- 로고/앱명: `Vinyl-Check`
- 짧은 설명 문구
- 알림 아이콘
  - 알림 화면으로 이동
- 검색창
  - 홈 화면 안에서 즉시 필터링
- 검색어 삭제 아이콘

중간:

- 검색어가 있을 경우 검색 결과 개수 표시
- 검색 결과가 없을 경우 빈 상태 안내
- 상품 레일이 세로로 나열됨

표시되는 리스트:

- 추천 감정 매물 리스트
  - 음질 점수 85점 이상 또는 초반 추정 매물
- 최근 등록 매물 리스트
  - 등록 목록 순서 기반
- 희귀판/초반 리스트
  - `isRare` 매물
- 전체 상품 리스트
  - 전체 매물

각 리스트 구성:

- 리스트 제목
- 더보기 링크
- 가로 스크롤 상품 카드
- 상품 카드 내용
  - 앨범 커버
  - 희귀 표시
  - 내 상품 표시
  - 찜 버튼
  - 앨범명
  - 아티스트
  - 가격
  - 음질 등급/점수

상품 클릭 시:

- 상품 상세 화면으로 이동

```text
/app/album/:id
```

하단:

- 공통 탭바
  - 홈
  - 검색
  - 판매
  - 찜
  - 프로필

연결 기능:

```text
GET /listings
```

구현 상태:

- 서버 매물 목록 로드 시도
- 서버 실패 시 mock 매물 사용
- 찜 기능은 앱 상태/localStorage 중심
- 알림 화면 이동 구현됨

미구현/부분구현:

- “인기 매물”이라는 별도 이름의 레일은 없음
- 현재는 “전체 상품”이 네 번째 레일임

보완안:

- 인기 매물 레일을 별도 추가
  - 기준 예시: 조회수, 찜 수, 채팅 수, 최근 거래 전환율
- 백엔드에 인기 정렬 API 추가

```text
GET /listings?sort=popular
```

---

## 6. 상품 상세 화면

경로:

```text
/app/album/:id
```

상단:

- 뒤로가기 버튼
- 공유 아이콘
- 신고 아이콘
  - 신고 화면으로 이동

중간:

- 앨범 이미지
- 이미지 페이지 점
- 앨범명
- 아티스트명
- 찜 버튼
- 연도 / 장르 / 조회수
- 내 상품 또는 거래 완료 상태 표시
- 희귀판/초반 태그
- Vinyl-Check 감정 카드
  - 음질 등급
  - 음질 점수
  - 자켓 상태
  - 초반 추정 여부
- LP 샘플 영역
  - 3초짜리 임시 음원 재생
- 자켓 정보
  - 카탈로그 번호
  - 발매 연도
- 가격 영역
  - 판매 가격
  - 시세 범위
  - 시세보다 낮음/높음 안내
- 상세 설명
- 판매자 정보
  - 판매자 이름
  - 평점
  - 거래 수
  - 판매자 프로필 이동
- 거래 위치

하단:

- 일반 구매자일 때
  - 문의하기
  - 채팅하기
- 내 상품일 때
  - 제안
  - 댓글/문의
  - 채팅

연결 기능:

- 신고 이동:

```text
/report/album/:id
```

- 판매자 프로필 이동:

```text
/app/profile/:sellerId
```

- 채팅 이동:

```text
/transaction/chat/:id
```

구현 상태:

- 상세 UI 구현됨
- 판매자 프로필 이동 구현됨
- 신고 이동 구현됨
- 채팅 이동 구현됨

미구현/부분구현:

- 공유 버튼은 UI만 있고 실제 공유 기능 없음
- LP 샘플은 실제 업로드 음원이 아니라 브라우저 오실레이터 임시 소리
- 상세 화면은 현재 `mockAlbums` 기준으로 앨범을 찾음
  - 서버에서 등록한 신규 매물과 상세 화면 연동이 완전하지 않을 수 있음

보완안:

- Web Share API 또는 Capacitor Share 플러그인 연결
- 서버 매물 상세 API 추가

```text
GET /listings/{listing_id}
```

- 실제 오디오 샘플 파일 저장/재생 기능 추가

---

## 7. 검색 화면

경로:

```text
/app/search
```

상단:

- 뒤로가기 버튼
- 검색 입력창
- 검색어 삭제 버튼
- 검색 버튼

중간:

- 최근 검색어 목록
  - 검색어 클릭 시 결과 화면 이동
  - 개별 삭제
  - 전체 삭제
- 추천 검색어 목록
  - 버튼 형태

하단:

- 공통 탭바 영역 안에서 열린 경우 탭바 유지

연결 기능:

- 검색 결과 화면 이동

```text
/app/search/results?q=...
```

구현 상태:

- 검색어 입력/추천 검색 이동 구현됨

미구현/부분구현:

- 최근 검색어가 실제 저장소에 영구 저장되지 않음
- 추천 검색어는 고정 배열

보완안:

- 최근 검색어 localStorage 저장
- 서버 인기 검색어 API 추가

---

## 8. 검색 결과 화면

경로:

```text
/app/search/results
```

상단:

- 뒤로가기 버튼
- 검색창
- 필터 버튼
- 정렬 버튼

중간:

- 필터 패널
  - 장르
  - 가격 범위
  - 음질 등급
  - 희귀반만 보기
  - 지역
- 정렬 패널
  - 최신순
  - 낮은 가격순
  - 높은 가격순
  - 음질 등급 높은 순
  - 인기순
- 결과 개수 표시
- 세로 상품 리스트
  - `AlbumCard` 반복

하단:

- 공통 탭바 영역 안에서 열린 경우 탭바 유지

연결 기능:

- 상품 클릭 시 상세 화면 이동
- 찜 토글

구현 상태:

- 검색/필터/정렬 클라이언트 처리 구현됨
- 인기순은 `views` 기준 정렬

미구현/부분구현:

- 서버 검색 API가 아니라 앱 메모리 목록에서 필터링

보완안:

```text
GET /listings?q=&genre=&price_min=&price_max=&grade=&rare=&sort=
```

형태로 백엔드 검색/필터 API 추가

---

## 9. 찜 화면

경로:

```text
/app/favorites
```

상단:

- 제목: 찜한 상품
- 찜한 상품 개수

중간:

- 찜한 상품이 있으면 상품 카드 리스트
- 없으면 빈 상태 안내

하단:

- 공통 탭바

구현 상태:

- 찜 목록 UI 구현됨
- 앱 상태 기반 찜 토글 구현됨

미구현/부분구현:

- 현재 첫 진입 시 찜이 비어 있으면 mock 상품 3개를 자동으로 넣음
- 실제 사용자별 서버 찜 API 없음

보완안:

```text
GET /users/{user_id}/favorites
POST /users/{user_id}/favorites/{listing_id}
DELETE /users/{user_id}/favorites/{listing_id}
```

---

## 10. 알림 화면

경로:

```text
/app/notifications
```

상단:

- 제목: 알림

중간:

- 알림 리스트
  - 제안 알림
  - 채팅 알림
  - 관심 상품 알림
  - 새 매물 알림
- 읽지 않은 알림은 배경 강조와 점 표시
- 알림 클릭 시 읽음 처리
- 링크가 있으면 해당 화면으로 이동

하단:

- 공통 탭바 영역 안에서 열린 경우 탭바 유지

구현 상태:

- mock 알림 기반 UI 구현됨
- 클릭 시 읽음 처리 구현됨

미구현/부분구현:

- 실제 푸시 알림/서버 알림 저장소 없음
- 앱에 Push Notifications 패키지는 있으나 실질 연결은 안 됨

보완안:

- 알림 테이블/API 추가
- FCM 또는 Capacitor Push Notifications 연결

---

## 11. 프로필 화면

경로:

```text
/app/profile
/app/profile/:userId
```

상단:

- 제목: 프로필
- 내 프로필일 경우 설정 아이콘

중간:

- 프로필 카드
  - 아바타 원형 영역
  - 사용자명
  - 평점
  - 거래 수
  - 이메일 인증 상태
- 통계 3칸
  - 리뷰 평균
  - 리뷰 수
  - 판매 중 수
- 선호 장르 태그
- 다른 사용자 프로필일 경우 채팅하기 버튼
- 탭 영역
  - 판매 목록
  - 리뷰
- 판매 목록 탭
  - 판매 상품 카드 리스트
  - 판매 중/거래 완료 태그
- 리뷰 탭
  - 리뷰어 이름
  - 별점
  - 코멘트
  - 앨범명

하단:

- 공통 탭바

연결 기능:

```text
GET /users/{user_id}/reviews
GET /users/{user_id}/review-summary
GET /listings
```

구현 상태:

- 리뷰 목록과 요약 실제 백엔드 연결됨
- 내 프로필은 실제 로그인 사용자 정보 우선
- 신규 가입자는 기본 평점/거래수 0

미구현/부분구현:

- 프로필 사진 업로드 없음
- 팔로우/차단/신뢰 배지 같은 확장 기능 없음
- 판매 목록은 서버 목록과 mock 목록 혼합 구조라 상세 연동이 부분적일 수 있음

보완안:

- 프로필 이미지 업로드 API 추가
- 판매자별 매물 API 추가

```text
GET /users/{user_id}/listings
```

---

## 12. 설정 화면

경로:

```text
/app/settings
```

상단:

- 뒤로가기 버튼
- 제목: 설정

중간:

- 프로필 설정
  - 닉네임
  - 선호 장르 선택
  - 임시 저장 버튼
  - 저장 결과 메시지
- 화면 모드
  - 라이트
  - 다크
  - 시스템
- 알림 설정
  - 채팅 알림
  - 가격 제안 알림
  - 희귀반 알림
- 거래 설정
  - 기본 거래 지역
  - 가격 제안 허용
  - 최소 제안가 슬라이더
- 인증 상태
  - 이메일 인증 여부
- 로그아웃 버튼

하단:

- 스크롤 끝에 로그아웃 버튼

연결 기능:

```text
PUT /users/{user_id}/profile-draft
```

구현 상태:

- 프로필 저장 서버 연결됨
- 설정은 localStorage 기반 저장
- 테마 전환 구현됨
- 로그아웃 구현됨

미구현/부분구현:

- 알림 설정은 실제 푸시 서버와 연결되어 있지 않음
- 거래 설정은 실제 매물 등록/제안 조건과 완전 연동되지 않음

보완안:

- 사용자 설정 서버 저장 API 추가
- 가격 제안 가능 여부를 제안 화면에서 검증

---

## 13. 아이디 찾기 화면

경로:

```text
/auth/find-id
```

상단:

- 뒤로가기 버튼
- 제목: 아이디 찾기

중간:

- 가입 이메일 입력
- 결과 메시지 표시
- 아이디 받기 버튼

하단:

- 별도 하단 고정 영역 없음

연결 기능:

```text
POST /auth/find-id
```

구현 상태:

- 실제 백엔드 연결됨
- SMTP 설정이 있으면 이메일 발송
- SMTP 설정이 없으면 앱 화면에 마스킹 아이디 표시

미구현/부분구현:

- 메일 발송 성공/실패 이력 저장 없음

보완안:

- SMTP 또는 이메일 서비스 실제 연결
- 발송 제한/보안 로그 추가

---

## 14. 비밀번호 찾기 화면

경로:

```text
/auth/reset-password
```

상단:

- 뒤로가기 버튼
- 제목: 비밀번호 찾기

중간:

- 이메일 또는 아이디 입력
- 재설정 코드 받기 버튼
- 재설정 코드 입력
- 새 비밀번호 입력
- 비밀번호 변경 버튼
- 결과 메시지 표시

하단:

- 별도 하단 고정 영역 없음

연결 기능:

```text
POST /auth/password-reset/request
POST /auth/password-reset/confirm
```

구현 상태:

- 실제 백엔드 연결됨
- 재설정 코드 생성
- 코드 만료 시간 30분
- SMTP가 없으면 앱 화면에 코드 표시
- 코드와 새 비밀번호로 해시 갱신

미구현/부분구현:

- SMS 인증 없음
- 재설정 시도 횟수 제한 없음

보완안:

- 시도 횟수 제한
- IP/계정 기준 rate limit
- 재설정 완료 후 자동 로그인 여부 정책 결정

---

## 15. 판매 등록 화면

경로:

```text
/sell
```

상단:

- 뒤로가기 버튼
- 제목: 판매 등록
- 게시 버튼
  - 조건 충족 시 활성화

중간:

- 사진과 표면 분석
  - 자켓 이미지 업로드
  - 자켓 상태 분석 결과
  - 표면 이미지 업로드
  - 표면 동영상 업로드
  - 촬영 가이드
  - 표면 상태 점수
  - 스크래치 후보 개수
  - 재생 영향
- 음질 녹음 분석
  - Discogs 트랙 기반 녹음 추천
  - 좋은 구간 녹음
  - 잡음 확인 구간 녹음
  - 녹음 파일 재생
  - 음질 분석 실행
  - 음질 등급/점수/노이즈/클릭 후보 표시
- 판매 정보
  - 카탈로그 번호
  - Discogs 검색
  - 후보 선택
  - 앨범명
  - 아티스트
  - 매트릭스 번호
  - 판매 가격
  - 추천 가격
  - 상세 설명
  - 태그
- 감정 요약
  - 표면 점수
  - 자켓 점수
  - 음질 점수
  - 감정 결과 확인 체크
- 임시 저장

하단:

- 화면 자체 하단 탭바 없음
- 게시 버튼은 상단에 위치

연결 기능:

```text
GET /discogs/search
GET /discogs/track-recommendations
POST /analysis/lp-recognition
POST /analysis/jacket-condition
POST /analysis/audio-samples
PUT /users/{user_id}/listing-draft
POST /listings
```

구현 상태:

- 판매 등록 플로우 대부분 실제 백엔드 연결됨
- 서버 실패 시 로컬 fallback 분석 사용
- 임시 저장 구현됨
- 게시 후 판매 리포트 화면으로 이동

미구현/부분구현:

- 실제 이미지 파일 서버 업로드와 CDN URL 저장은 완전하지 않음
- 등록 후 상세 화면이 서버 매물 상세로 완전 연결되지 않을 수 있음
- 음질 분석은 서버 분석 실패 시 파일 크기 기반 fallback

보완안:

- 이미지 업로드 API와 매물 생성 API를 강하게 연결
- 상세 화면을 서버 매물 기준으로 수정
- 분석 실패/성공 여부를 사용자에게 명확히 표시

---

## 16. 촬영 화면

경로:

```text
/sell/camera
```

상단:

- 뒤로가기 버튼

중간:

- 검은 배경
- 앨범 자켓을 프레임에 맞추라는 안내
- 사각 프레임 가이드

하단:

- 이미지 아이콘 버튼
- 촬영 버튼
- 오른쪽 빈 공간

구현 상태:

- UI만 구현됨
- 촬영 버튼 클릭 시 실제 카메라 캡처가 아니라 판매 등록 화면으로 돌아감

미구현:

- 실제 카메라 프리뷰
- 실제 사진 촬영
- 촬영 결과 전달

보완안:

- Capacitor Camera 연결

```ts
Camera.getPhoto(...)
```

- 촬영 결과를 판매 등록 draft에 저장

---

## 17. 감정 요청 화면

경로:

```text
/sell/analysis
```

상단:

- 뒤로가기 버튼
- 제목: 감정서 만들기

중간:

- 감정 자료 안내 카드
- 매트릭스 번호 입력
- 자켓 사진 업로드/촬영
- LP 표면 이미지/동영상 업로드
- 스크래치 후보 표시
- 촬영 가이드
- 표면 상태 확인 카드
- 카탈로그 번호 입력
- Discogs 검색
- 앨범 후보 선택
- 자켓 상태 측정

하단:

- 감정 결과 만들기 버튼

연결 기능:

- 판매 등록 화면과 같은 분석/Discogs 함수 사용
- 감정 리포트는 localStorage성 저장소에 저장 후 결과 화면 이동

구현 상태:

- 분석 요청 플로우 구현됨
- 이미지/동영상 업로드 가능
- 표면 스크래치 후보 SVG 표시 구현됨

미구현/부분구현:

- 감정서가 서버 DB에 저장되는 독립 API는 없음
- 사진 촬영은 별도 카메라 화면이 UI 수준

보완안:

```text
POST /analysis/reports
GET /analysis/reports/{report_id}
```

추가

---

## 18. 감정 결과 화면

경로:

```text
/sell/analysis/result
```

상단:

- 뒤로가기 버튼
- 제목: LP 감정 결과

중간:

- 자켓 이미지와 감정 배지
- 앨범 후보 정보
- LP 표면 이미지
- 스크래치 후보 표시
- 표면 점수
- 자켓 점수
- 자켓 등급
- 표면 상태 근거
  - 스크래치 후보
  - 스크래치 위험도
  - 재생 영향
  - 반사 위험도
- 자켓/초반 근거
  - 카탈로그 번호
  - 레이블
  - 발매국
  - 발매 연도
  - 매트릭스 번호
  - 초반 추정
  - 희귀도
- 자켓 감점 사유
- 음질 분석 결과
- 가격 영향
- 추천 시작 가격

하단:

- 판매 등록 폼에 적용 버튼
- 판매 등록으로 돌아가기 버튼

구현 상태:

- 감정 결과 리포트 UI 구현됨
- 결과를 판매 등록 draft에 반영 가능

미구현/부분구현:

- PDF/이미지 감정서 내보내기 없음
- 서버 저장 없음

보완안:

- 감정서 PDF 생성
- 판매글 상세에 감정서 링크 표시

---

## 19. 판매 리포트 화면

경로:

```text
/sell/report
```

상단:

- 뒤로가기 버튼
- 제목: 판매 리포트

중간:

- 현재 판매 가격 카드
- 평균 시세
- 가격 범위
- 가격 추이 그래프
- 게시 상태 카드
- 분석 인증 카드
- 가격 제안 안내

하단:

- 확인 버튼
  - 홈 화면으로 이동

구현 상태:

- UI 구현됨
- 게시 완료 후 이동 가능

미구현/부분구현:

- 가격 리포트 데이터는 현재 고정 mock 데이터
- 실제 등록 상품의 가격/분석값과 일부만 연결

보완안:

```text
GET /pricing/report/{listing_id}
```

추가 후 실제 시세/조회/제안 데이터를 표시

---

## 20. 채팅 화면

경로:

```text
/transaction/chat/:chatId
```

상단:

- 뒤로가기 버튼
- 상대/판매자 이름
- 실시간 연결 상태

중간:

- 거래 상품 요약 카드
  - 커버
  - 앨범명
  - 아티스트
  - 가격
- 거래 완료 확인 카드
  - 구매자 확인
  - 판매자 확인
  - 둘 다 확인 시 리뷰 화면 이동
- 메시지 리스트
  - 내 메시지 오른쪽
  - 상대 메시지 왼쪽
  - 보낸 시간 표시

하단:

- 이미지 버튼
- 가격 제안 버튼
- 메시지 입력창
- 전송 버튼

연결 기능:

```text
GET /chats/{chat_id}/messages
WebSocket /chats/{chat_id}/ws
```

구현 상태:

- 메시지 히스토리 API 연결
- WebSocket 실시간 채팅 연결
- 연결 실패 시 재연결 시도
- 거래 완료 확인 후 리뷰 화면 이동

미구현/부분구현:

- 이미지 전송 버튼은 UI만 있음
- 거래 완료 확인은 localStorage 기반
- 채팅방 ID가 앨범 ID와 섞여 쓰임

보완안:

- 채팅방/거래 ID 모델 분리
- 이미지 업로드 후 채팅 메시지에 첨부
- 거래 완료 상태 서버 저장

---

## 21. 상품 문의 화면

경로:

```text
/transaction/comments/:albumId
```

상단:

- 뒤로가기 버튼
- 제목: 상품 문의
- 앨범명
- 문의 개수

중간:

- 판매자일 경우 답변 영역
  - 구매자 문의 선택
  - 답변 템플릿 선택
  - 직접 답변 입력
  - 답변 등록
- 문의 리스트
  - 작성자
  - 구매자 문의/판매자 답변 태그
  - 날짜
  - 내용
  - 판매자 답변 박스

하단:

- 문의 입력창
- 전송 버튼

구현 상태:

- 문의 작성 UI 구현
- 판매자 답변 UI 구현
- 답변 템플릿 구현

미구현/부분구현:

- 문의/답변은 로컬 상태 mock
- 서버 저장 없음

보완안:

```text
GET /listings/{id}/comments
POST /listings/{id}/comments
POST /comments/{comment_id}/reply
```

---

## 22. 가격 제안 화면

경로:

```text
/transaction/offer/:albumId
```

상단:

- 뒤로가기 버튼
- 제목: 가격 제안

중간:

- 상품 요약 카드
- 시세 카드
- 판매가 카드
- 제안 가격 입력
- 프리셋 버튼
  - 판매가
  - 5% 낮게
  - 10% 낮게
- 제안 상태 안내
  - 너무 낮은 제안
  - 합리적인 제안
  - 수락 가능성 높음

하단:

- 가격 제안 보내기 버튼

구현 상태:

- UI와 가격 판단 로직 구현
- 제출 시 alert 후 이전 화면 이동

미구현:

- 서버에 가격 제안 저장 안 됨
- 판매자에게 실제 알림 안 감

보완안:

```text
POST /offers
GET /users/{user_id}/offers/received
PATCH /offers/{offer_id}
```

---

## 23. 받은 가격 제안 화면

경로:

```text
/transaction/offers/received
```

상단:

- 뒤로가기 버튼
- 제목: 받은 가격 제안

중간:

- 받은 제안 리스트
  - 상품 이미지
  - 구매자 이름
  - 앨범명
  - 시간
  - 현재 가격
  - 제안 가격
- 제안 상태
  - pending
  - accepted
  - rejected
- 수락/거절 버튼

하단:

- 별도 하단 고정 버튼 없음

구현 상태:

- mock 제안 리스트 구현
- 수락 시 localStorage 거래 상태 저장

미구현/부분구현:

- 실제 서버 제안 목록 아님
- 거절/수락이 서버에 저장되지 않음

보완안:

- 가격 제안 API와 연결
- 수락 시 실제 거래 생성 API 호출

---

## 24. 진행 중 거래 화면

경로:

```text
/transaction/ongoing/:transactionId
```

상단:

- 뒤로가기 버튼
- 제목: 진행 중인 거래

중간:

- 거래 진행 중 안내 카드
- 상품 요약
- 거래 상대 정보
- 채팅 버튼
- 약속 정보
  - 일시
  - 장소
  - 지도 버튼
- 안전 거래 안내

하단:

- 채팅하기 버튼
- 거래 완료 버튼
- 거래 취소 버튼

구현 상태:

- UI 구현
- 버튼 이동 구현

미구현/부분구현:

- 거래 정보가 고정 mock 데이터
- 실제 거래 ID 기반 조회 없음

보완안:

```text
GET /transactions/{transaction_id}
PATCH /transactions/{transaction_id}
```

---

## 25. 거래 장소 화면

경로:

```text
/transaction/location/:transactionId
```

상단:

- 뒤로가기 버튼
- 제목: 거래 장소

중간:

- 지도 영역 placeholder
- 빨간 위치 핀

하단:

- 장소명
- 주소
- 길찾기 버튼
- 확인 버튼

구현 상태:

- 화면 UI 구현

미구현:

- 실제 지도 SDK 연결 없음
- 길찾기 버튼 실제 동작 없음

보완안:

- Kakao Map 또는 Google Maps SDK 연결
- 길찾기 deep link 연결

---

## 26. 거래 취소 화면

경로:

```text
/transaction/cancel/:transactionId
```

상단:

- 뒤로가기 버튼
- 제목: 거래 취소

중간:

- 취소 경고 안내
- 취소 사유 라디오 목록
- 기타 사유 입력

하단:

- 돌아가기 버튼
- 거래 취소 버튼

구현 상태:

- UI 구현
- 제출 시 alert 후 홈 이동

미구현:

- 서버 거래 취소 처리 없음
- 상대방 알림 없음

보완안:

```text
POST /transactions/{transaction_id}/cancel
```

---

## 27. 리뷰 작성 화면

경로:

```text
/transaction/review/:transactionId
```

상단:

- 뒤로가기 버튼
- 제목: 거래 후기 작성

중간:

- 거래 상품 요약
  - 이미지
  - 앨범명
  - 아티스트
  - 판매자
- 별점 선택
  - 1~5점
- 별점별 안내 문구
- 거래 경험 태그 선택
- 상세 후기 입력
- 결과 메시지

하단:

- 후기 등록 버튼

연결 기능:

```text
POST /reviews
GET /users/{user_id}/reviews
GET /users/{user_id}/review-summary
```

구현 상태:

- 실제 리뷰 저장 API 연결됨
- 저장 후 판매자 프로필로 이동
- 리뷰 평균/리뷰 수 갱신됨

미구현/부분구현:

- 동일 거래 중복 리뷰 방지 로직 없음
- 리뷰 수정/삭제 없음

보완안:

- `transactionId + reviewerId` 기준 중복 체크
- 리뷰 수정/삭제 API 추가

---

## 28. 신고 화면

경로:

```text
/report/:type/:id
```

상단:

- 뒤로가기 버튼
- 제목: 신고하기

중간:

- 허위 신고 경고
- 신고 사유 라디오 목록
  - 사기/허위 매물
  - 중복 게시물
  - 거래 금지 물품
  - 가격 허위 기재
  - 욕설/비방
  - 개인정보 노출
  - 기타
- 상세 내용 입력

하단:

- 신고 제출 버튼

구현 상태:

- UI 구현
- 제출 시 alert 후 이전 화면 이동

미구현:

- 신고 서버 저장 없음
- 관리자 검토 화면 없음

보완안:

```text
POST /reports
GET /admin/reports
PATCH /admin/reports/{report_id}
```

---

## 29. 공통 하단 탭바

적용 화면:

```text
/app
/app/search
/app/search/results
/app/album/:id
/app/favorites
/app/notifications
/app/profile
/app/settings
```

하단 구성:

- 홈
- 검색
- 판매
- 찜
- 프로필

구현 상태:

- 탭 이동 구현됨
- 현재 탭은 파란색으로 강조

미구현/부분구현:

- 상세 화면이나 설정 화면에서도 탭바가 유지될 수 있어 화면 깊이에 따라 UX가 다소 복잡할 수 있음

보완안:

- 상세/설정/알림 같은 depth 화면에서는 탭바 숨김 여부 검토
- 또는 탭바 유지 정책을 명확히 디자인 문서화

---

# 전체 기능 구현 상태 요약

## 실제 백엔드와 연결된 기능

- 로그인
- 회원가입
- 아이디/이메일 중복 확인
- Google 로그인 백엔드 처리
- 아이디 찾기
- 비밀번호 재설정
- 매물 목록 로드
- 매물 등록
- 판매글 draft 저장
- 프로필 draft 저장
- 리뷰 등록
- 리뷰 목록/요약 조회
- 채팅 히스토리
- WebSocket 채팅
- Discogs 검색
- 트랙 추천
- 표면 분석
- 자켓 분석
- 음질 분석

## mock/localStorage 중심 기능

- 홈 일부 추천 로직
- 찜 목록
- 알림 목록
- 문의/답변
- 가격 제안
- 받은 가격 제안
- 진행 중 거래
- 거래 장소
- 거래 취소
- 신고
- 판매 리포트 가격 차트
- 감정 결과 저장

## UI는 있으나 실질 기능이 부족한 것

- 실제 카메라 촬영 화면
- 공유 버튼
- 이미지 채팅 전송
- 실제 지도
- 실제 푸시 알림
- 신고 서버 처리
- 가격 제안 서버 처리
- 거래 상태 서버 처리
- 인기 매물 독립 섹션
- 실제 오디오 샘플 저장/재생

---

# 우선 구현 추천 순서

## 1순위: 서버 저장이 필요한 거래 기능

가격 제안, 거래 생성, 거래 완료, 거래 취소를 서버에 저장해야 앱이 “거래 앱”처럼 일관되게 동작한다.

필요 API:

```text
POST /offers
GET /users/{user_id}/offers/received
PATCH /offers/{offer_id}
POST /transactions
GET /transactions/{transaction_id}
PATCH /transactions/{transaction_id}
POST /transactions/{transaction_id}/cancel
```

## 2순위: 매물 상세 서버화

현재 상세 화면은 `mockAlbums` 기반 의존이 남아 있다. 서버 등록 매물이 상세 화면에서도 완전히 열리도록 바꿔야 한다.

필요 API:

```text
GET /listings/{listing_id}
```

## 3순위: 카메라/업로드 실사용화

판매 등록의 핵심은 사진/영상/음성 자료이므로 실제 파일 업로드와 카메라 촬영이 안정적으로 연결되어야 한다.

필요 작업:

- Capacitor Camera 연결
- 이미지/영상/음성 업로드 API 정리
- 매물 생성 시 업로드 URL 저장

## 4순위: 문의/신고/알림 서버화

커뮤니티와 안전 기능은 localStorage가 아니라 서버에 남아야 한다.

필요 API:

```text
POST /reports
GET /listings/{id}/comments
POST /listings/{id}/comments
GET /notifications
PATCH /notifications/{id}/read
```

## 5순위: 홈 화면 구성 정리

사용자가 요청한 구조에 맞추려면 홈 화면은 다음 네 개 섹션이 명확해야 한다.

- 추천 매물 리스트
- 최근 등록 매물 리스트
- 희귀판 매물 리스트
- 인기 매물 리스트

현재 네 번째는 “전체 상품”이므로 “인기 매물”로 바꾸거나, 전체 상품과 인기 매물을 둘 다 제공할지 결정해야 한다.

